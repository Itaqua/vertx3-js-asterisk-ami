/**
 * Octavio
 * list of MQS addresses user in all the project (client/server isomorphic)
 */

const messages = "messagesapp/messages"
const messagesEvent = `${messages}/event`

export default {
  messages,
  messagesEvent
}